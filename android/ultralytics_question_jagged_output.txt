Our SHARP 3D Gaussian splatting pipeline on Android (ExecuTorch INT8) produces a completely jagged, screwed-up 3D reconstruction instead of a coherent room. The rendered scene is chaotic: stretched and compressed shapes, extremely jagged edges, no smooth surfaces. It does not resemble a room at all.

**Exact run (logs):**
- Input image: 3840x2160 (landscape, 16:9)
- We letterbox to 1536x1536 (content 1536x864 centered, black bars top/bottom)
- aspectRatio=1.7777778, isPortrait=false
- In writePly we apply: x = rawX * aspectRatio, z = -rawZ * aspectRatio, y = -rawY
- Output: 1,179,648 Gaussians; reported room bbox 21.85 x 12.30 x 12.12 m
- The UI shows "21.9 x 12.3 m" but the visual is completely broken (jagged, terrain-like, not a room)

**What we need to know:**

1. **Aspect correction direction:** For landscape 16:9 we currently multiply x,z by aspectRatio (1.78). Could that over-stretch and cause jaggedness? Should we divide by aspectRatio for landscape instead, or use a different formula (e.g. scale only one axis)?

2. **INT8 quantization:** Could INT8 quantization of the SHARP model produce such severe geometric corruption (jagged positions, scales, or rotations)? If so, what export or runtime changes reduce artifacts—e.g. per-channel vs per-tensor, or falling back to FP16 for the decoder?

3. **Coordinate scale:** The model outputs (x,y,z) and we write them with the aspect scale above. Are we misinterpreting the model’s coordinate system or units (e.g. normalized vs meters), leading to wrong scale and jagged rendering?

4. **Letterbox and patch grid:** Our 5x5 patch grid samples the 1536x1536 canvas; the actual image content is only 1536x864 in the center. Could patches overlapping the black bars corrupt the latent or decoder output and cause the jagged result?

5. **Gaussian count:** 1.18M Gaussians is large. Could the viewer (Three.js / splat renderer) or our PLY write be incorrect for very large counts (e.g. buffer overflow, wrong stride, or precision) and produce visual garbage?

Please give concrete, actionable fixes (code or config changes) for the Android Kotlin pipeline or the Python export. This is 3D Gaussian splatting (SHARP), not YOLO.
