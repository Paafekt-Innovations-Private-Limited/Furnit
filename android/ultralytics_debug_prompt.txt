# Ultralytics debug prompt – aspect distortion (SHARP INT8 ExecuTorch)

When the 3D room still looks wrong after letterbox + aspect correction, paste the prompt below into Ultralytics Ask AI (docs.ultralytics.com) and fill in your logs and symptom.

---

We use ExecuTorch INT8 for SHARP 3D Gaussian splatting on Android. We already:
- Letterbox input to 1536x1536 (long side = 1536, black padding).
- Apply aspect correction in writePly: x = rawX * aspectRatio, z = -rawZ * aspectRatio (y = -rawY).
- Viewer uses Box3 mapping: landscape roomWidth=size.x, roomDepth=size.z; portrait roomWidth=size.z, roomDepth=size.x, 90° Y rotation for portrait.

**Problem (describe one):**
[ ] Landscape: room looks squeezed / narrow
[ ] Portrait: room looks expanded or snake-like wavy edges on walls/objects
[ ] Other: _________________________________

**Relevant logs from ExecutorchInt8Sharp (filter logcat by tag ExecutorchInt8Sharp or grep for [ASPECT] [LETTERBOX] [PLY]):**

```
[ASPECT] input=___x___ | isPortrait=___ | aspectRatio=___
[LETTERBOX] input=___x___ -> content=___x___ on 1536x1536 (pad horizontal=___ px vertical=___ px)
[PLY] writePly: count=___ aspectRatio=___ isPortrait=___ (applying x,z *= aspectRatio)
[PLY] saved bbox: roomWidth=___ roomHeight=___ roomDepth=___
[ASPECT] inference complete | Gaussians=___ | PLY bbox room=___x___x___ m
```

**Question:** Given these values and the symptom above, what should we change next? Options we consider: (1) Different aspect formula (e.g. inverse or per-axis scale for portrait). (2) Swap or rotate x,z in writePly when isPortrait to match viewer 90° Y. (3) Letterbox scale strategy (e.g. short side = 1536 and pad long side). (4) Model export / training aspect. Not YOLO; this is 3D Gaussian splatting room reconstruction.
