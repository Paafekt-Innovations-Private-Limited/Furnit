We have a SHARP ExecuTorch INT8 pipeline on Android that outputs a 3D Gaussian splat room (PLY). We switched to stretch-to-square (full image 3840x2160 -> 1536x1536, no crop) and write PLY with raw coordinates only (x = rawX, y = -rawY, z = -rawZ; no aspect scaling). Room bbox from the last run: roomWidth=8.79, roomHeight=7.22, roomDepth=4.19 m. We also added a wide-angle (0.5x) camera inset adjustment in the viewer. Despite that, the default camera position in the 3D viewer still does not look correct—no visible change.

**Viewer setup:**
- Three.js + Spark (Gaussian splat) + OrbitControls in a WebView.
- We place the camera at the back wall (box.max.z - inset or box.max.x - inset for portrait), target at front wall center. We use Box3 from the loaded splat mesh for backWall/frontWall when size is valid; otherwise fallback to roomWidth/roomHeight/roomDepth passed from Kotlin.
- For portrait we apply splatMesh.rotation.y = Math.PI/2 so room aspect matches the photo; then Box3 gives size and we map roomWidth=size.z, roomHeight=size.y, roomDepth=size.x.

**Problem:** The initial view still appears in the wrong position (room or camera not correctly framed). Changing the camera formula (inset, wide-angle multiplier) did not fix it.

**Questions for Ultralytics:**
1. **PLY coordinate frame:** For a ViT-based 3D Gaussian model (SHARP), are the raw (x,y,z) outputs in a canonical room frame (e.g. origin at room center, or at camera/viewpoint)? Should the viewer translate the mesh so the room center is at world origin before computing Box3 and camera position? Could the model output an offset origin that makes our back-wall camera formula wrong?
2. **Box3 vs actual geometry:** Spark/Three.js Box3 is computed from the splat mesh after load. Could the mesh bounds update asynchronously so that our first camera frame uses wrong min/max and never gets updated? We already retry autoFrameRoom when size is tiny.
3. **Axis and sign:** We negate y and z when writing PLY. For "camera at back, looking at front", is the correct interpretation that +Z is "back" (camera side) and -Z is "front" (photo wall), or the opposite? Could wrong front/back mapping make the camera appear to be on the wrong side of the room?
4. **Best practice for default view:** What is the recommended way to set the initial camera and target for a single-photo 3D reconstruction viewer so the room is consistently framed (back-wall view looking at front wall), regardless of PLY origin or axis convention from the model?

We are not using YOLO; this is SHARP 3D Gaussian splatting. We need concrete suggestions: e.g. center the mesh before framing, use a different axis for depth, or compute camera from bbox center + depth direction.
