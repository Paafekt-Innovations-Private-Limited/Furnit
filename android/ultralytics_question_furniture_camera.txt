We have a 3D Gaussian splat room viewer (Three.js, OrbitControls, SparkJS). The room is reconstructed from a single photo (SHARP/ViT). We want the initial camera to follow this priority:

1. Look at furniture first (if any furniture is known).
2. If no furniture, look at a wall (e.g. back wall or main wall).

We do not have per-object or per-furniture 3D positions from the pipeline—only room bbox, dimensions, and bbox center. We can optionally pass a single 3D "focus point" (e.g. from future furniture detection or user tap).

What is the recommended way to choose the initial camera look-at target in this case?
- Should we use room center as a proxy for "furniture" (typical room center often has furniture), and only use a wall when we have an explicit "empty room" or "look at wall" flag?
- For "look at a wall" fallback: which wall is best (back wall vs front wall) for single-view reconstruction where the camera/viewer is placed inside the room at eye height?
- Any Ultralytics or best-practice guidance for 3D scene viewers: prioritize framing on content (furniture/objects) over empty walls?
